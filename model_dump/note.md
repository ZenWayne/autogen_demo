## dump the mcp servergroup chat config
1. use the AssistantAgent.dump_component() method to dump the config
2. use termination condition to terminate the chat
3. use mcp server as tool







